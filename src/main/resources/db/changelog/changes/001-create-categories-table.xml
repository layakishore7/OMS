<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="1" author="dev">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="categories"/>
            </not>
        </preConditions>

        <createTable tableName="categories">
            <column name="id" type="INT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="shipper_id" type="INT">
                <constraints nullable="false"/>
            </column>

            <column name="category_name" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>

            <!-- Parent category reference (nullable for root categories) -->
            <column name="parent_id" type="INT">
                <constraints nullable="true" foreignKeyName="fk_category_parent"
                             references="categories(id)"/>
            </column>

            <column name="record_status" type="INT" defaultValueNumeric="1">
                <constraints nullable="false"/>
            </column>

            <column name="created_at" type="TIMESTAMP"/>
            <column name="updated_at" type="TIMESTAMP"/>
        </createTable>

        <addForeignKeyConstraint baseTableName="categories" baseColumnNames="shipper_id"
                                 referencedTableName="organizations" referencedColumnNames="id"
                                 baseTableSchemaName="public" referencedTableSchemaName="public"
                                 constraintName="fk_categories_shipper"
                                 onUpdate="CASCADE" onDelete="RESTRICT"/>

        <!-- Optional: enforce unique category names within the same parent -->
        <addUniqueConstraint
                tableName="categories"
                columnNames="parent_id, category_name"
                constraintName="uk_category_parent_name"/>

        <!-- Optional: index for faster parent-child queries -->
        <createIndex indexName="idx_category_parent_id" tableName="categories">
            <column name="parent_id"/>
        </createIndex>

    </changeSet>

</databaseChangeLog>
